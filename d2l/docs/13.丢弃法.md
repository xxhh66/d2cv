# torch框架使用

### 1. 为什么要写 `forward()`

在 PyTorch 中，**所有继承 `nn.Module` 的模型都必须实现 `forward()`**。

```python
output = model(images)
```

实际上，PyTorch内部并不是直接调用模型，而是

```python
output = model.forward(images)
```

`forward() `定义了数据在神经网络中的传播过程。

```python
def forward(self, x):
    x = self.features(x)
    x = self.classifier(x)
    return x
```

含义：

```bash
输入图片
      │
      ▼
features（卷积层）
      │
      ▼
classifier（全连接层）
      │
      ▼
输出10个类别
整个VGG11的前向传播流程。
```

### 2.网络搭建，训练和测试都要使用`forward`函数

```python
# __init__只在创建时执行一次：
model = VGG11()
# 换句话说，会调用，定义模型结构 + 初始化参数。
def __init__(self, num_classes=10) 

# 每输入一次数据，就执行一次
def forward(self, x):

    # 训练时：
    for images, labels in train_loader:
    	outputs = model(images) # forward(images)
    # 测试时：
    with torch.no_grad():
    	outputs = model(images) # forward(images)
```

3. 训练`epoch`，计算损失

```python
# 训练函数
def train_epoch():
    model.train()

    total_loss = 0
    correct = 0
    total = 0

    for images,labels in train_loader:
        images = images.to(device)
        labels = labels.to(device)

        # 前向传播
        outputs = model(images)
        loss = criterion(outputs,labels) #模型预测结果（outputs）和真实标签（labels）之间的差距
        
        # 反向传播
        optimizer.zero_grad()
        loss.backward()
        optimizer.step()

        # 统计
        total_loss +=loss.item()*images.size(0)

        _,predicted = outputs.max(1)
        correct += predicted.eq(labels).sum().item()
        total += labels.size(0)

    avg_loss = total_loss/total
    acc = correct/total
    return avg_loss,acc
```

其中 `loss = criterion(outputs,labels) `是模型预测结果（outputs）和真实标签（labels）之间的差距
第一步：模型预测

```python
outputs = model(images)
```

这里执行了 `forward()`。假设有一张图片是真实标签为 **T-shirt（类别0）**。

模型输出可能是：

```python
outputs = [-0.5, 2.1, 0.8, 1.5, -0.3, -1.0, 0.4, 0.1, -0.8, -2.0]
```

注意：

这里**不是预测类别**。

而是10个类别的**得分（logits）**。

例如

| 类别        | 得分 |
| ----------- | ---- |
| 0(T-shirt)  | -0.5 |
| 1(Trouser)  | 2.1  |
| 2(Pullover) | 0.8  |
| 3(Dress)    | 1.5  |
| ...         | ...  |

最大的值是

```
2.1
```

对应类别

```
1（Trouser）
```

如果直接预测：

```
pred = outputs.argmax(dim=1)
```

模型认为：

> 这是一条裤子。   



最终`loss`值：先计算softmax

```python
# 1 模型给出概率logits
outputs = [0.2,4.5,1.0]
# 2. 计算softmax
e^0.2 +e^4.5 +e^1.0 
=1.221+90.017+2.718
=
93.956
#3. 类别0的概率
1.221/93.956 ≈0.013
# 4.真实标签 0 
# 5.计算损失 loss,最终的loss -4.34
loss = -log(0.013)≈-4.34
```

​      

### 3.  loss.backward() 

   ```python
   # 计算模型所有参数的梯度
   loss.backward()
   
   # loss.backward() 不是计算 loss，而是计算 loss 对模型所有参数的梯度（Gradient）。为了让 loss 变小，每个参数应该往哪个方向改？改多少？
   ```



### 4.optimizer.step()                

```python
optimizer.step()                 # 更新参数
```


$$
weight = weight-lr*grad
$$
为什么要减去梯度：梯度表示，**Loss 增长最快的方向。**

如果梯度是正的：往右走，Loss 越来越大。因此要往左走：$weight = weight - lr * grad$

如果梯度是负的：往左走，Loss 越来越大。因此要往右走：$weight = weight - (-0.5)$